# Skill 自检清单

## 结构

- [ ] `literature-reading/SKILL.md` 存在且仅含 `name`、`description` frontmatter 字段。
- [ ] `modules/`、`domains/`、`templates/`、`schemas/` 与 `agents/openai.yaml` 存在。
- [ ] `literature-workspace/` 必需目录与入口文件齐全。
- [ ] Skill 与个人工作区分离。

## 行为

- [ ] 快速模式不会未经请求输出超长精读。
- [ ] 深读模式保存阶段状态和继续位置。
- [ ] 五类严格溯源标签完整且不会混淆。
- [ ] 外部知识有来源并限制在最小必要范围。
- [ ] 普通公式、关键算法、证明深度符合约定。
- [ ] 论文符号、统一符号、维度/单位和代码形状均可记录。
- [ ] 未读取四篇论文时不虚构关系和排序。

## Zotero Markdown

- [ ] 行内公式只使用 `$...$`。
- [ ] 独立公式使用单独的 `$$` 行。
- [ ] 不使用 `\(...\)` 或 `\[...\]`。
- [ ] 标题、列表、表格和公式前后有空行。
- [ ] 层级树可脱离 Mermaid 独立理解。

## 数据安全

- [ ] 修改前读取已有文件，只更新相关部分。
- [ ] 跨论文关系、创新、研究问题和知识等级提升先待确认。
- [ ] 冲突与不确定性被保留。
- [ ] 重要更新写入 `changelog.md`。

## 论文集验收

- [ ] 横向表包括 ADP 分类、理论保证、机械臂证据和代码情况。
- [ ] 同时保留导师顺序与自适应顺序。
- [ ] 预备课由论文共同依赖驱动。
- [ ] 完成后可生成 Zotero 双版本笔记与阶段复盘。

## Git

- [ ] 仓库根目录包含 `.gitignore`、`VERSION` 和 `GIT-WORKFLOW.md`。
- [ ] PDF、数据集、权重、缓存和外部仓库默认不被跟踪。
- [ ] Skill 变更验证后通过 `scripts/sync-skill.ps1` 部署。
- [ ] 回退优先使用 `git revert`，不使用破坏性 reset。
